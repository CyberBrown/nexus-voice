# TTS Quality Testing & Tuning System Dependencies
# Install with: pip install -r requirements.txt

# Core audio processing
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0

# Pitch extraction (Praat Python bindings)
praat-parselmouth>=0.4.3

# Speaker embeddings
speechbrain>=1.0.0

# Deep learning (should be pre-installed in NVIDIA container)
torch>=2.0.0
torchaudio>=2.0.0

# HTTP client for TTS API
requests>=2.28.0

# For transformers-based TTS (if using direct model loading)
transformers>=4.35.0
safetensors>=0.4.0
accelerate>=0.24.0

# For metrics visualization (optional)
matplotlib>=3.7.0

# Progress bars (optional)
tqdm>=4.65.0
